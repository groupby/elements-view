defaults:
  docker: &docker
    - image: cirlceci/node:11.6.0

version: 2

jobs:
  build:
    <<: *docker
    steps:
  unit_test:
    build:
    <<: *docker
    steps:

workflows:
  version: 2

  build_test:
